#include <bits/stdc++.h>
#define ll long long

using namespace std;

ll t;
ll n;
ll H , M ;
ll h[100100];
ll m[100100];

void start()
{
    cin >> t;
    memset(h,0,sizeof(h));
    memset(m,0,sizeof(m));
}


void repeat()
{
    ll idx = 0;
    while (idx<t)
    {

    }
}

void solve()
{

    cin >> n >> H >> M ;
    for (ll i=0 ;i<n ; i++)
    {
        cin >> h[i] >> m[i];
    }
}
int main()
{
    start();

    return 0;
}
